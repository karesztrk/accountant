import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  row: {
    cursor: "pointer",
  },
  selectionCell: {
    width: "40px",
  },
  actionCell: {
    width: "40px",
  },
  addButton: {
    margin: "0 auto",
  },
}));
